# 更新日志

## 0.8.2（正式签名 Release，待允许发布）：审查修复第一批

- 任务页响应只唤醒统一自动任务入口，不再直接使用缺少请求身份的宿主响应执行任务；遵守每日完成、执行中和失败冷却限制。
- 解析成功但业务未完成时不再重置失败轮数；所有本地上报失败也进入冷却。
- 分享入口仅调用一次原调用链，宿主异常不再被捕获后重放。
- 手动任务上报失败显示明确提示；手动与自动路径均排除 `blocking`，统一完成状态大小写处理。
- 旧配置迁移保留宿主中已有键，不覆盖用户的新设置。
- 原图按钮的正常跳过不再清除运行故障；只在按钮实际处理点击后记录派发恢复，不宣称下载成功。
- 仅 Wi-Fi 模式在实际点击前复查网络；不再通过其他 Wi-Fi 网络猜测默认 VPN 的承载。
- 推荐视频 Hook 按每个成功注册的位置计数，局部失败不再少记已有 Hook。
- 新增静态回归检查；这不是手机行为测试，也不代替实际验收。
- 发布规则调整为先推送源码，用户测试并明确批准后才发布 APK。

### 仍待后续批次处理

- 首页刷新回归（本批不改动）。
- 原图真实加载完成信号、异步失败恢复与有限重试。
- 手动任务点击去重与更完整的原操作回退；任务来源白名单及逐项明确完成状态确认。
- 确认请求及延迟任务统一取消、主线程持久化迁移到有序后台流程。
- 业务网页路由边界、信息流副本读写契约与更新点过滤优化。

本批不上传 APK，不创建 Release。

## 0.8.0（2026-09-04）

### 修复

- 重做每日分享任务的持久化状态机。
  - 不再在任务真正完成前写入“今日已完成”。
  - 使用 `IN_PROGRESS / CONFIRMED` 区分执行中和服务器已确认。
  - 进程被杀或部分任务失败后，下次启动重新读取服务器，只处理仍待完成的任务。
  - 旧版本只有日期的记录不再被当作可信完成态，会先向服务器核实。
- 任务解析增加结构识别、检查数量和逐项失败计数。
  - 结构变化或任务 Getter 失效时进入有限重试，不再把解析失败误判成“没有任务”。
  - 服务端确认阶段使用相同的可靠性判断，避免错误显示全部完成。
- 每日任务日期改为小黑盒服务器使用的中国自然日语义，不受设备时区变化影响。
- 捕获并取消宿主 RxJava `Disposable`，超时、重试、账号变化和流程结束时不再保留旧请求。
- 修复同一信息流 List 等量原位更新后仍返回旧过滤结果的问题。
- 信息流缓存改为 O(1) 弱身份映射，不再依赖可变数据模型的 `equals/hashCode`。
- “返回首页不自动刷新”只拦截页面恢复触发的过期刷新；再次主动点击首页仍可正常回顶部。
- 外部浏览器没有明确默认项时改用系统选择器，不再随机打开第一个 URL 处理应用。
- 广告 Getter 增加返回类型校验，并返回独立可变空列表，避免宿主修改共享不可变列表时崩溃。

### 自检

- 复合 Hook 组记录实际安装数与预期数。
- 广告、媒体、外部浏览器、返回首页刷新、开屏广告和更新提示可显示 `x/y` 部分安装状态。
- 自检结果新增“部分安装”，不再因一组中只有一个 Hook 成功而显示完整。

### 版本兼容区

- 暂停版本伪装、在线获取版本和主动版本检测入口。
- 旧配置中的版本伪装开关不会安装任何版本身份或网络参数 Hook。
- 当前只保留独立的“屏蔽版本更新弹窗”。

### 签名迁移

- Release 不再使用机器 debug key，改为项目固定正式签名。
- 没有提供固定签名配置时只生成 unsigned APK，不会静默回退 debug 签名。
- 由于签名证书变化，`0.7.5` 及更早版本不能直接覆盖升级到 `0.8.0`，需要先卸载旧模块。

### 构建

- `versionName`: `0.8.0`
- `versionCode`: `33`
- Modern Xposed API: `102`

## 0.7.5（2026-09-04）

### 修复

- 修复每日自动分享任务在用户切换页面时，服务端偶尔只确认两个任务的问题。
  - 三个分享上报之间的间隔由 450ms 调整为 900ms，降低宿主埋点队列和服务端批处理窗口互相覆盖的概率。
  - 保留两次服务端状态确认；若仍有本轮任务未完成，只对未确认项补报一次并再次确认。
  - 正常一次完成三个任务时不会产生补报请求，也不会改变“每个账号每天只自动执行一次”的规则。
- 修复多图查看器中只有第一张图片能自动加载原图的问题。
  - 不再使用依赖 `MediaData.equals/hashCode` 的弱哈希集合；这些字段会在加载过程中变化，曾导致后续图片状态无法命中。
  - 改为弱引用对象身份集合，不持有已经离开的图片页面。
- 修复快速滑动图片时，已离开的页面仍可能延迟触发原图加载的问题。

### 优化

- 自动查看原图改为按当前页懒加载：滑到哪张，只在该张普通图片与原图元数据均准备好后加载原图。
- 不预加载相邻图片，避免长图集产生额外流量、内存占用和解码卡顿。
- 新增定向补报日志 `DAILY_TASK_REPAIR_*`，便于区分初次上报与恢复流程。

### 构建

- `versionName`: `0.7.5`
- `versionCode`: `32`
- Modern Xposed API: `102`
