# 项目协作与发布约束

- 源码和文档统一使用 UTF-8，不修改 `jadx_src/` 中的反编译资料。
- 每批修复或更改经检查后，只提交并推送源码、测试和文档。
- 不自动创建或更新 GitHub Release，不上传 APK、AAB 或测试包。
- 必须等待用户测试，并取得用户针对 APK 发布的明确允许，才能上传 APK。
- 如生成本地测试 APK，更新版本号并复制到 `X:\临时同步`；这不代表获得发布许可。
- 不操作手机应用进行测试。ADB 仅在用户授权的范围内读日志或安装，不自行执行 UI 操作。
- 当前“返回首页不自动刷新”存在用户报告的回归，未经验证不得宣称手动刷新正常。
